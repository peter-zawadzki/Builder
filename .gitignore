node_modules/
dist/
.env
.env.local
google-service-account.json
builder-update-agent-*.json
*.iam.gserviceaccount.json
.DS_Store
coverage/
.vite/

# Raw Resource Center asset drop zone — a staging/archive area, not what's
# actually served. The curated, served copies live in public/resource-assets/
# and are committed normally.
/resource-assets/
/Demo/
/alex/
/Sales Tools/
/new3k/

# Same pattern — raw legal PDF drop zone; served copies live in
# public/legal/ and are committed normally.
/legal/
